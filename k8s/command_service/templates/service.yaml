apiVersion: v1
kind: Service
metadata:
  name: {{ include "command_service.fullname" . }}
  labels:
    {{- include "command_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "command_service.selectorLabels" . | nindent 4 }}
