apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "command_service.fullname" . }}-test-connection"
  labels:
    {{- include "command_service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "command_service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
