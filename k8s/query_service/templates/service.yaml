apiVersion: v1
kind: Service
metadata:
  name: {{ include "query_service.fullname" . }}
  labels:
    {{- include "query_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "query_service.selectorLabels" . | nindent 4 }}
